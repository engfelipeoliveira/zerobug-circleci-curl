version: 2.1
jobs:
  build:
    docker: 
      - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: curl -k -u $ZEROBUGUID:$ZEROBUGTOKEN --request POST -H "Content-Type:application/json" -d '{"pit":"130","targetid":"68bWPqdDNqkIy9NTloGu1598197167","ci":"Cicle CI","tipo":"6"}' https://zerobug.co/ScanTarget/
